class AppUrl {
  static var baseUrl = 'https://reqres.in/';
  static var loginUrl = baseUrl + 'api/login';
  static var signUrl = baseUrl + 'api/register';

  // this is the Url class past all url here;
}
